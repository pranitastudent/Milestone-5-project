from django.shortcuts import render

# Create your views here.

# def size_filter(request):
#     if request.method == "POST"
#       for i in request.form:
#           if i == "size"
#               filter_items = []
#               items = request.form.getlist('size')
#     return render_template('filter.html', ?)              
    